# SPIFFS web UI snapshot

Copy of `plantbot-hardware/data/Updater` for release asset `spiffs-data.zip`.

Upload contents of `Updater/` to the device as described in
`docs/releases/v1.3.0-alpha.md` (curl or `/manager`).
